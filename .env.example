# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database (optional: only if using external DB)
# DATABASE_URL=postgresql://user:password@localhost/dbname
